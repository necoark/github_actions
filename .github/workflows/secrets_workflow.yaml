name: vars workflow
on: [push]

jobs:
  run-command:
    runs-on: ubuntu-latest
    steps:
      - name: my vars and secrets
        run: |
          echo "${{ vars.STATUS }}"
          echo "${{ secrets.PASSWORD }}"
